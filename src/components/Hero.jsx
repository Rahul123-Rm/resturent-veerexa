import React from 'react';

const Hero = () => (
  <section className="hero">
    <div className="hero__overlay" />
    <div className="hero__content">
      <h1 className="hero__title">Welcome to Veerexa Restaurant</h1>
      <p className="hero__subtitle">Experience premium dining with us</p>
      <a href="#menu" className="hero__cta">Explore Menus</a>
    </div>
  </section>
);

export default Hero;